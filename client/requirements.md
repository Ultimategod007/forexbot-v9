## Packages
framer-motion | Smooth animations for page transitions and hover states
embla-carousel-react | Carousel slider for the featured series banner
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
lucide-react | Beautiful icons

## Notes
- Images are handled via external URLs for this MVP (users paste URLs).
- Replit Auth is used for authentication.
- Dark mode is the default and primary aesthetic.
